export const constRights = {
    ACCOUNT_AVAILABLE: 8,
    FIAT_CURRENCY: 4,
    EXCHANGE_AVAILABLE: 2,
    STRUCT_INVEST_AVAILABLE: 1,
}

export const testRightsUser = (value, $const) => value >= $const